<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.agostinho.sicredimobile">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".conta.ContaFormularioActivity" />
        <activity android:name=".main.PrincipalActivity" />
        <activity android:name=".transacao.SaldoActivity" />
        <activity android:name=".transacao.ExtratoActivity" />
        <activity android:name=".transacao.DepositoActivity" />
        <activity android:name=".transacao.SaqueActivity"></activity>
    </application>

</manifest>